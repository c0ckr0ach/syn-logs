{
  "dataset_name": "generic",
  "description":  "Generic / unknown log format — format is fully auto-detected from the sample file",

  "model_id": "qwen",

  "generation": {
    "target_logs":        1000,
    "batch_size":         20,
    "temperature":        0.85,
    "max_new_tokens":     1024,
    "anomaly_rate":       0.15,
    "sample_window_size": 30,
    "output_file":        "synthetic_generic_logs.txt",
    "checkpoint_file":    "checkpoint_generic.txt",
    "checkpoint_every_n": 200,
    "write_labels":       true
  },

  "log_format": {
    "auto_detect": true
  }
}
